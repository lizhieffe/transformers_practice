{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Python Debugger: Current File with Arguments",
            // "type": "debugpy",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": false, // Whether to debug only user code. Set to false to debug all code (including libraries).
            "env": {
                "PYTHONPATH": "${workspaceFolder}:/home/lizhi/development/transformers/src:$PYTHONPATH"
            }
        }
    ]
}